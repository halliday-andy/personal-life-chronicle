import GlobeClient from '@/components/globe/GlobeClient'

// The residential globe (Step 7 Slice 1). Auth is enforced by the
// (protected) layout. Full-bleed nocturne surface.
export default function GlobePage() {
  return <GlobeClient />
}
