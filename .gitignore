.DS_Store
**/.DS_Store
*.swp
*.swo

# Next.js
.next/
out/
node_modules/

# TypeScript incremental build cache (per-machine, regenerated by tsc)
*.tsbuildinfo

# Environment — never commit secrets
.env.local
.env.*.local

# Supabase local state
supabase/.branches
supabase/.temp

# Claude Code — machine-local settings (settings.json IS tracked; .local is not)
.claude/settings.local.json
